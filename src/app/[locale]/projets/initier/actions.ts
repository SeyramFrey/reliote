"use server";
import type { ProjectInput } from "@/lib/validation/project.schema";

export async function submitProject(_input: ProjectInput) {
  return { error: "Not implemented yet — wired in Task 35" };
}
